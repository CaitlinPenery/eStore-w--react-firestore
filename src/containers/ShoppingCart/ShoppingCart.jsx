import React from "react";

import CartItem from "../../components/CartItem";

const ShoppingCart = () => {
    return (
        <div>
            <CartItem />
        </div>
    );
};

export default ShoppingCart;
