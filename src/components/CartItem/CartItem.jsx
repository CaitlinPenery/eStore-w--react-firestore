import classes from "./CartItem.module.scss";
import { useState } from "react";
import { getCart, updateCart, updateProduct } from "../../services/products";

const CartItem = () => {
    return (
        <div>
            <h2>Cart Items</h2>
        </div>
    );
};

export default CartItem;
